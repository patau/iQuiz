platform :ios, '9.0'
use_frameworks!

target 'iQuiz' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for iQuiz
  pod 'RadioButton', '~> 1.0'
  pod 'SQLite.swift', '~> 0.11.3'

  target 'iQuizTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'iQuizUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
